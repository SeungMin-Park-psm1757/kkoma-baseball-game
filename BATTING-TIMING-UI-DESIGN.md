# 타자 타이밍 UI 설계안 — 먼저 설계만, 구현은 다음 단계

## 1. 문제 정의
현재 `swing()`은 투구 진행률 `(now - state.pitch.start) / state.pitch.duration`이 0.88인 지점을 타격 중심으로 사용합니다. 아이는 공의 위치를 눈으로 판단해야 하지만 타격 타이밍을 알려주는 사전 신호가 부족합니다. 우측 상단 3×3 `contactFeedback` 격자는 타격 **후** 결과만 보여 줍니다.

## 2. 권장 설계: 타격 지점 링 + 실시간 타이밍 게이지
- **A. 경기장 안 홈플레이트 부근의 얇은 타격 링:** 투구 공이 타격 지점에 다가오면 링이 부드럽게 밝아집니다. 공을 가리거나 플레이어/포수 이미지 위에 진한 박스를 그리지 않습니다. 링의 정점은 실제 판정 중심인 진행률 0.88입니다.
- **B. 경기장 바로 아래, '지금 쳐요!' 버튼 바로 위의 가로 게이지:** 공의 진행을 표시하는 이동 마커와 추천 구간을 함께 표시합니다. 회색(준비) → 노랑(타격 가능) → 초록(정확한 타이밍), 마커가 추천 구간에 들어가면 '지금!' 안내를 한 번 표시합니다. 실시간 판정값을 별도로 재계산하지 않고 `swing()`과 동일한 `state.pitch.start / duration / center / window`을 사용합니다.
- **C. 타격 이후:** 게이지와 링을 바로 숨기고 기존 우측 상단 타격 분석 3×3 격자에서 맞힌 위치·정확도를 보여 줍니다. 헛스윙은 '조금 빨라요/늦었어요' 피드백을 보여 줍니다.

## 3. 판정값과 위치 일치
- 공의 진행률 `p = clamp((now - start) / duration, 0, 1)`
- 판정 중심 `center = 0.88`
- 타격 허용 `window = levels[state.level].window + characters[state.character].bonus`
- 현행 파울 경계 `hitHalfWidth = window * (1 - levels[state.level].foulChance * 2)`
- 화면의 노랑/초록 범위를 현재 레벨·캐릭터에 맞게 계산. 추천 초록 구간은 `center ± min(0.075, hitHalfWidth * 0.45)`로 **허용 구간보다 좁게** 표시해 아이에게 명확한 목표를 제공합니다. 현재 타격 성공·파울·스트라이크 계산 자체는 바꾸지 않습니다.
- 1레벨 예시 `pitchMs=2000`: 타격 중심은 1760ms. 6레벨 `pitchMs=1250`: 중심은 1100ms. 각 레벨의 진짜 공 속도에 동기화합니다.

## 4. 갤럭시 노트10+ 세로 화면 기준
화면 순서: [점수판] → [기존 16:9 경기장 + 얇은 링] → [폭 100% 실시간 타이밍 게이지] → [기존 큰 타격 버튼]. 게이지 높이는 20~25 CSS px 수준, 터치 대상이 아님. 320~430px에서 텍스트·버튼과 겹치지 않게 별도 줄을 확보합니다. 색 외에도 마커 위치와 '준비/지금!' 텍스트를 써서 색상 인지에만 의존하지 않습니다.

## 5. 구현 전 테스트 명세
1. 6레벨과 3캐릭터에서 표시된 초록 추천 구간이 실제 안타/타격 허용 구간 안에 있을 것.
2. 시간 정지·일시정지 재개 시 게이지와 실제 투구 시간이 함께 멈추고 재개할 것.
3. 헛스윙·파울·타구·투수 모드 전환 때 표시를 숨기고 기존 3×3 분석 격자를 유지할 것.
4. 모바일 320px 및 갤럭시 노트10+ 세로 화면에서 점수판·버튼과 겹치지 않을 것.
5. 난이도나 주루·송구 수치는 변경하지 않을 것.

## 6. 음악 검증(동일 브랜치)
- 업로드된 `Little Devils' Playbook.mp3`의 Git blob SHA는 `2f14dcf0a71d9fb1776e6b3b8dca0129553d7222`. 저장소의 `assets/music/level-6-devils.mp3`와 **바이트 단위로 동일**합니다. 따라서 인트로의 파일 경로를 기존 파일로 변경했습니다.
- 승리곡 `Home Run Celebration (Option 1).mp3`의 blob SHA는 `95b43177fbf3c3597654657d2bca2263d87b5191`이고, 저장소 내 일치하는 파일은 없습니다. 코드는 `assets/music/home-run-celebration.mp3`를 기다리고 있으므로 이 MP3를 별도로 로컬 Git 커밋/푸시해야 합니다.
- 크롬 모바일은 사용자 입력 전 소리 있는 자동재생을 차단할 수 있습니다. 인트로는 첫 일반 터치에 재생을 재시도하도록 기존 코드가 처리합니다.

**초기 문서의 음악 검증 기록은 당시 부모 브랜치 기준이며, 최신 테라 브랜치의 변경사항은 8절을 참고하세요.**

## 7. 화면별 설계 대안 비교 (추가 검토)

| 대안 | 무엇이 보이는가 | 장점 | 유의점 |
|---|---|---|---|
| ① 가로 타이밍 게이지 | 경기장 아래 진행 마커가 우측으로 움직이며 노랑·초록 영역에 접근 | 타격 시점을 가장 수치에 가깝게 알 수 있고 6레벨 속도에 연동 가능 | 시선이 공과 게이지 사이를 오갈 수 있음 |
| ② 공 주변 수축 링 | 공이 홈플레이트에 가까워질수록 링이 수축해 정확한 타이밍에 일치 | 시선을 경기장에 유지하고 야구의 타격감이 좋음 | 모바일의 작은 공과 링이 겹치면 산만할 수 있음 |
| ③ 타격 버튼의 예고·점등 | 버튼 테두리가 노랑으로 변하고 정확한 시점에 초록색과 '지금!' 표시 | 노트10+ 세로 조작 시 가장 직관적, 별도 공간 적게 사용 | 이미 반응한 순간에야 불이 켜지면 늦게 누를 수 있으므로 사전 예고가 중요 |
| ④ 타격 존 3×3 사전 격자 | 접촉 부근에 작은 3×3 격자와 진입 예상 지점 표시 | 타격 위치와 타이밍을 함께 학습 가능 | 화면 복잡도와 오해 가능성이 커서 1학년 어린이의 1차 안내로는 부적합 |

**권장 초기 구현:** ①(예고를 포함한 진행 게이지) + ③(기존 타격 버튼의 동기 점등)을 조합. 실제 공의 위치와 눈에 띄는 목표 사이의 학습을 돕고, 게이지는 경기장 아래에 두어 캐릭터나 공을 가리지 않게 합니다. ②는 선택형 시각효과로 후속 검증하고, ④는 기존 타격 후 격자에 남깁니다.

**타이밍 계산:** 0.88 중심, 레벨별 window와 캐릭터 보너스, 파울 경계는 같은 값을 공유합니다. 초록색은 정확한 타격의 추천 영역이고 노랑 영역 전체가 반드시 안타를 보장하는 것은 아닙니다. 공의 비행과 판정은 그대로 유지합니다.

## 8. 테라 프리뷰 브랜치의 음악 상태
`feature/tera-music-and-preview`에서 `assets/music/home-run-celebration.mp3` 업로드를 확인했습니다. 인트로는 기존 `level-6-devils.mp3`를 사용합니다. 2026-09-21 변경으로 홈 → 캐릭터 → 경기장 선택 구간에서 같은 재생 위치를 유지하며, 실제 경기에 들어갈 때만 인트로를 정지하고 레벨별 음악으로 전환합니다. 최초 자동재생 제한이 있으면 선택 화면의 첫 일반 터치/키 입력에서도 재생을 시도합니다.

**현재는 음악 코드 수정만 구현했으며, 타격 타이밍 UI ①~④는 설계 대안입니다.**
