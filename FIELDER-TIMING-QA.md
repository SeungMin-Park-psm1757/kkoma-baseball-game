# PR #6 검증 기록 — 수비 보행 · 베이스 방향 · 타격 링

검증 대상: `feature/fielder-motion-and-timing-ring` (부모 `feature/tera-music-and-preview`). `main` 변경·병합 없음.

## 발견 및 수정

**수비 보행 레이어 순서:** 최초 구현은 Canvas에서 `drawFielderRunLegs()`를 먼저 그리고 그 위에 전신 `drawOpponentFrame()`를 그려 움직이는 다리가 가려질 가능성이 컸습니다. 렌더 순서를 바꿔 전신 이미지 다음에 보행선을 그리도록 수정하고 회귀 검사로 고정했습니다. 새 이미지 생성이나 이동 속도 변경은 하지 않았습니다.

## 코드·모의 실행 검증

| 항목 | 결과 |
|---|---|
| JavaScript 초기화/구문 | PASS |
| 게임 내 자기점검 | 88개 PASS, 오류 0개 |
| 새 수비 보행 애니메이션 | 115ms 간격 프레임 변경 및 전신 이미지 **이후** 다리 그리기 확인 |
| 1루·2루 대기 주자 | 두 위치 모두 `baseIdleFlip()`에서 반전하고, 정적/도착 직후 동일 함수 사용 |
| 3루 대기 주자 | 좌우 반전 없이 홈 방향의 기존 배치를 유지 |
| 타격 링 | 실제 타격 중심 `BATTING_TIMING_CENTER=.88`에서 수축·최대 강조 확인 |
| 일시정지 | 고정된 시간값으로 타격 링 위치 유지 |
| 타격 직후/투수 모드 | 타격 링이 표시되지 않음 |
| 주루·송구 | 6개 레벨을 순회하는 1,000가지 자동 주루·송구 시나리오 통과 |
| 인트로·승리 MP3 | `assets/music/level-6-devils.mp3`, `assets/music/home-run-celebration.mp3` 모두 작업 브랜치 트리에 존재 |

## 실제 기기에서 추가 확인 필요

- 115ms 인공 다리 선이 기존 캐릭터 다리와 겹쳐 어색해 보이지 않는지, 장면별 착지·발 교차 자연스러움.
- 1루·2루 수비수의 원본 해상도는 교체하지 않고 스케일(+7%)·그림자·Canvas smoothing을 조정한 것임. 고화질 자산 확보 여부에 따른 시각적 한계가 남을 수 있음.
- 2루 주자 `runnerStand` 실제 원본의 정면 방향에 대한 시각 확인.
- 갤럭시 노트10+ 세로 화면의 링 크기, 소리 재생, 터치 타이밍.
- 실제 브라우저 UI 스크린샷/눈으로 보는 플레이 검증은 이번 실행에서 수행하지 않았음. 모의 Canvas/Audio 검사만으로 최종 영상 품질을 보장하지 않음.

최종 병합과 프로덕션 배포는 사용자 확인 이후 수행할 것.
