# 모바일 세로 화면 · 결과 카드 · 음악 적용 안내

## 기준
- 주요 기준: 갤럭시 노트10+의 세로 화면(실제 CSS viewport는 브라우저 배율과 UI 표시 여부에 따라 달라짐). 폭 약 412 CSS px를 기준으로 반응형 규칙을 작성했으며 320~760px 대응 및 낮은 화면 높이에서 압축 배치를 지원.
- 경기장 Canvas의 내부 좌표는 `960 × 540`(16:9)을 **그대로 유지**합니다. 베이스·송구·타구 좌표를 왜곡하지 않고, **게임 전체 UI를 세로 흐름**으로 재배치합니다. 경기장 자체를 9:16으로 크롭/회전하는 변경은 하지 않았습니다.
- 경기장 위: 경기 제목 및 점수판 / 경기장 아래: 피드백·베이스 상태·넓은 타격 버튼. 투수 조준 3×3 버튼도 세로 흐름으로 표시.
- 결과 페이지: 이미지·배지·제목·설명·점수/홈런 또는 실점/아웃 카드·큰 이동 버튼을 하나의 카드 내에 배치. 성공과 실패 이미지는 유지하고, 결과에 따라 배지 색상을 변경합니다.

## 음악
이 브랜치에는 음악 경로와 화면별 재생 로직이 들어 있습니다. **원본 MP3 바이너리는 별도로 업로드해야 합니다.**

| 화면/상태 | 저장할 파일 경로 | 처리 |
|---|---|---|
| 홈 인트로 | `assets/music/little-devils-playbook.mp3` | 약 2분 57초, 반복 재생 |
| 경기 중 | 기존 `assets/music/level-*.mp3` | 기존 레벨 BGM 사용 |
| 승리 결과 | `assets/music/home-run-celebration.mp3` | 약 1분 3초, 1회 재생 |
| 실패 결과 | 없음 | 승리 음악 미재생 |

음악 재생은 페이지 전환 시 다른 음악을 정지하도록 구성했습니다. 소리 끄기 버튼은 전체 BGM·효과음을 제어합니다. 모바일 브라우저가 첫 방문 시 자동재생을 막을 수 있으므로, 홈의 `♪ 인트로 음악 듣기` 버튼으로 사용자 동작 후 재생할 수 있습니다.

### 두 MP3를 GitHub에 업로드하는 방법
1. 음악 업로드 ZIP을 압축 해제합니다. 게임 저장소의 `feature/mobile-portrait-results-and-music` 브랜치로 이동합니다.
2. `assets/music/` 폴더의 `Add file → Upload files`에서 ZIP에 포함된 두 MP3를 파일명 그대로 추가합니다.
3. 이 브랜치에 커밋하면 연결된 PR에 파일 변경이 나타납니다.
4. 로컬에서는 새 브랜치를 ZIP으로 다운로드해 압축 해제한 뒤, 음악 ZIP의 `assets/music/` 폴더를 해당 게임 폴더로 복사·병합하면 됩니다. 이후 `index.html`을 Chrome에서 실행합니다.

## 테스트
- 코드 문법·CSS 괄호·UI 요소 연결 검사 통과.
- 모의 DOM/Audio/Canvas: 홈 인트로 시작 → 캐릭터 화면에서는 정지 → 경기에서는 기존 BGM → 승리 시 축하 음악 → 홈 복귀 시 인트로 → 실패 시 축하 음악 미재생 및 음소거 확인.
- 기존 자기점검 72개 통과 및 Canvas 1프레임 모의 실행.
- 실제 갤럭시 노트10+ 화면에서 스크롤/버튼 겹침 및 음악 플레이는 검수 필요.

## 병합 관계
`main ← PR #1 ← PR #2 ← PR #3 ← 이 브랜치` 순서입니다. 별도 ZIP으로 테스트할 경우 이 브랜치에는 이전 기능이 모두 포함됩니다.
