# 경기 재도전 전 한 문제 퀴즈

## 출처
승민님의 `SeungMin-Park-psm1757/game-for-son-3_v2` 저장소:
- `src/utils/UIManager.js`의 `showMathQuizSecondChance`, `showTypingQuiz`, `showSpellingQuiz`를 분석하여 별도 외부 게임 엔진 없이 브라우저용으로 재구현.
- `src/data/SpellingQuizData.js`의 받침 맞춤법 50문제를 `baseball-quiz.js` 상단에 그대로 이식.

## 게임 연결
- 경기 결과에서 **다음 경기**(승리하여 다음 레벨이 있을 때), **다시 하기**(승리·실패 모두)를 누르면 퀴즈 1개가 뜸.
- 결과에서 **처음으로**를 누르면 새 경기가 아니므로 바로 홈으로 이동.
- 유형은 숫자·맞춤법·한글 타이핑 3종 중 랜덤. 직전 유형만 제외하여 같은 유형의 연속 출현 방지.
- 두 차례까지 답할 수 있음. 오답을 두 번 선택하면 정답을 보여 준 뒤 경기 진행 허용. 추가 질문을 강제로 내지 않음.
- 기존 레벨별 난이도, 승패·점수·주루·송구·음악은 변경하지 않음.

## 문제 규칙
- 숫자: 원본 낚시게임 4장 규칙의 `10~30 + 5~20`, `15~35 - 5~15`, `2~5 × 1~9`를 동일하게 적용. 정답과 근접한 오답 3개를 중복 없이 제공. 첫 오답 시 다른 오답 하나 제거.
- 맞춤법: 원본 받침 퀴즈 50개 중 랜덤으로 문장의 `000`에 들어갈 말 고르기, 정답 포함 4지선다. 음성 기능 지원 브라우저에서는 문장 듣기 버튼 제공.
- 타이핑: 낚시게임의 한글 주문 단어 20개를 랜덤으로 가져와 정확히 입력. 모바일에서는 첫 화면에 키보드를 강제로 열지 않으며, 엔터/버튼 제출 모두 지원. 유니코드 NFC 정규화 후 비교.
- 버튼 크기 52~56px 및 2열 배치, 갤럭시 노트10+ 세로 화면 대응.

## 검증
- 숫자 1,200건: 연산식 정답, 4개 중복 없는 선택지, 범위 및 음수 방지 통과.
- 맞춤법 50문제 전부 정답이 선택지에 포함되어 있는지 확인.
- 퀴즈 36회 연속 실행: 매번 한 문제만, 다음 유형 중복 0회, 정답 뒤 진입 1회.
- 모의 DOM/Audio/Canvas에서 실제 야구게임과 결합: 실패→다시 하기, 승리→다음 경기, 실패→처음으로 이동 검사 통과.
- 기존 `game-v2.js` 자기점검 74개 통과.
- 실제 모바일 키보드·TTS·화면 겹침 등은 휴대전화에서 최종 시각 검수가 필요.

## 로컬 업데이트
현재 연결한 `feature/auto-music-batter-home-run` 브랜치의 작업 폴더에서 `git pull --ff-only`, 이후 `start .\\index.html`. `baseball-quiz.js`는 저장소에 포함되어 있어 별도 복사 불필요.
