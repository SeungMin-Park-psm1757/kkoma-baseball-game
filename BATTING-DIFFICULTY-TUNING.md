# 타격 모드 난이도 재조정 및 송구 판정 검토

## 2026-09-20 수정 범위

이 브랜치의 퀴즈·음악·홈런 연출·기존 수비 동작은 유지하고 타격 모드에서만 다음을 조정했습니다.

1. **타자주자 1루까지 가속:** `BATTER_FIRST_BASE_SPEED_BOOST = 1.025`를 설정했습니다. 이동 거리를 바꾸는 대신 첫 구간 소요시간을 `1/1.025`로 줄여 약 2.44% 빨리 도착합니다. 다중 베이스 이동 시에도 첫 구간에만 동일한 가속을 반영하고, 다른 주자의 속도 및 투수 모드 CPU 타자 속도는 유지합니다.
2. **1루 슬라이딩:** 타자주자의 1루 구간에서 진행률 90%부터 실제 `runner-3.png` 슬라이딩 프레임을 표시하고, 100% 도착 즉시 `runner-stand.png` 대기 프레임으로 전환합니다. 공이 먼저 도착한 경우의 아웃은 동일한 조정 시간에 따라 계산합니다.
3. **수비 송구 추가 2% 완화:** 기존 보정 1.02에 1.02를 다시 곱해 `BATTING_DEFENSE_TIME_FACTOR = 1.0404`로 변경했습니다. 기존 조정 직전 버전 대비 2% 증가, 최초 기준보다 약 4.04% 증가한 송구 시간입니다. 이 수치는 체감 난이도·승률이 정확히 2% 변한다는 뜻은 아닙니다.
4. **땅볼 포스아웃 판정:** 2·3루/홈으로의 첫 송구에서 선행주자의 실제 도착시간을 기준으로 아웃을 판정하도록 수정했습니다. 기존에는 타자주자의 1루 도착시간을 잘못 재사용했습니다. 후속 1루 병살은 별도로 타자주자의 도착시간을 사용합니다.
5. **외야 선송구 판단:** 테스트에서 2루 아웃이 가능한 빠른 포구에서는 2루→1루 송구, 이미 2루 주자가 안전한 늦은 포구에서는 1루 송구가 선택됨을 확인했습니다. 정당한 판단인 것으로 확인되어 송구 우선순위 코드는 변경하지 않았습니다. 회귀 검사를 추가했습니다.

## 검증 결과

- 문법 및 게임 내 자기점검 80개 통과.
- 외야수 위치·주자 배치·포구시각을 바꾼 1,000가지 자동 주루·송구 계산: 베이스 중복 및 송구 시각 오류 0건.
- 중견수 포구 시각 700ms: 2루→1루. 1,150ms: 이미 안전한 2루 대신 1루.
- 1루 도착: 기존 약 2,227ms → 약 2,173ms(정우 예시).
- 동일 송구 예시: 투수 모드 522ms 유지, 타격 모드 직전 532ms → 543ms.
- 모의 Canvas 렌더: 1루까지 진행률 88% `runner-1.png` → 92% `runner-3.png` → 도착 `runner-stand.png`.
- 실제 갤럭시/Chrome 시각 테스트와 난이도 체감은 별도 확인 필요.

## 업데이트
이미 연결된 `feature/auto-music-batter-home-run` 브랜치에서 `git pull --ff-only` 후 `start .\\index.html`을 실행합니다. 새 브랜치/ZIP은 필요 없습니다.
