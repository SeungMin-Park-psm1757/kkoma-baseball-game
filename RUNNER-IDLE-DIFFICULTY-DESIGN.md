# 주자 대기 모션과 타격 모드 미세 난이도 조정

## 의도
기존 `feature/batted-ball-bounces-and-contact-map`의 타격·바운드·플라이아웃·자동 주루·송구 및 투수 모드를 보존하면서, 주자가 각 베이스에 도착할 때 애니메이션을 종료하고 타격 모드 수비 송구 시간을 미세 조정합니다.

## 주자 표시 규칙
- 타구 후 베이스 간 실제 이동 구간에서는 기존 주행 스프라이트를 사용합니다.
- 도착 전 마지막 구간에서만 짧은 슬라이딩을 허용합니다. 이미 다음 베이스까지 갈 예정인 중간 구간에는 슬라이딩하지 않습니다.
- 베이스 도착 시각부터는 `assets/frames/runner-stand.png`를 사용합니다. 주행 중 상하 흔들림(bob)을 중단하고 베이스 위에 발 위치를 고정합니다.
- 추가 진루가 시작되면 해당 시각부터 다시 주행 프레임을 사용합니다.
- 아웃된 주자는 아웃 판정 시각 + 220ms 이후, 홈에 들어온 주자는 도착 시각 + 220ms 이후 화면에서 제거합니다.
- 타구 처리 완료 후 베이스에 있던 기존 주자 역시 같은 대기 스프라이트를 사용합니다.
- 아이가 조작할 별도의 주루 버튼이나 스탠딩 전환 버튼은 없습니다.

## 난이도 조정
`BATTING_DEFENSE_TIME_FACTOR = 1.02`를 도입하고 `defensiveThrowDuration()`에만 적용합니다. 타격 모드일 때 수비 송구 애니메이션과 세이프·아웃 계산에 동일한 송구 시간이 적용됩니다. 두 번 연속 송구에도 각 구간에 같은 보정이 들어갑니다. **투수 모드, 캐릭터 주루 속도, 타격 입력 구간, 타구 생성 확률은 변경하지 않습니다.**

이 보정은 **송구 소요시간 약 2% 증가**를 뜻합니다. 실제 승률·체감 난이도가 정확히 2% 낮아진다는 의미는 아닙니다.

## 검증
- JS 문법 검사, 6개 레벨의 타자·투수 모의 시작과 Canvas 렌더 통과.
- 자기점검 72개 assertion 모두 통과: 주자 도착 즉시 idle, 대기 유지, 홈 진입 및 아웃 후 제거, 타격/투수 모드 송구 시간 분리 포함.
- 모의 Canvas의 이미지 호출에서 달리는 중 `runner-1.png`, 도착 순간과 대기 중 `runner-stand.png` 확인.
- 주자 배치·외야수 위치·포구 시각 등을 달리한 자동 주루·송구 600개 검사 통과.
- 표본 송구: 타격 모드 532ms / 투수 모드 522ms(반올림 후 약 1.92% 차이).
- 실제 PC·모바일 플레이 화면의 체감 및 이미지 배치 확인은 아직 필요합니다.

## 병합 순서
1. PR #1 투수 조준·자동 주루 수정
2. PR #2 타구 바운드·타격 위치
3. 본 브랜치의 주자 모션·송구시간 수정

새 브랜치를 독립적으로 내려받아 실행하면 부모 브랜치의 기능을 모두 포함합니다.
