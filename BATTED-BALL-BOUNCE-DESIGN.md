# 꼬마 야구단 — 타구 바운드와 타격 위치 개선

## 이번 버전의 목표
기존 자동 주루·송구(2루 직송 후 1루 병살 포함), 3×3 투수 조준, 타격 버튼을 유지합니다. 타자의 버튼 입력 뒤 **타구를 만드는 과정**과 **공을 수비수가 확보하는 과정**을 더 다양하게 표현합니다.

## 타격 → 타구 → 수비 흐름
1. 타격 타이밍의 중심 오차를 타격 품질(0~100)과 격자 가로 지점으로 환산합니다. 랜덤한 세로 지점 및 방향·힘을 독립적으로 조합합니다.
2. 땅볼 / 낮은 직선타 / 강한 직선타 / 뜬공 / 높은 뜬공을 생성합니다. 타입(단타·2루타·3루타·홈런 가능성)과 구장 내 좌우 방향 및 도착 좌표를 조합하여 반복 패턴을 줄입니다.
3. **홈런 및 땅볼은 노바운드 포구가 불가능합니다.** 일반 직선타·뜬공은 포구 가능성을 별도로 계산합니다.
4. 노바운드 포구가 성립한 공은 첫 지면 접촉 이전에 수비수에게 도착하여 플라이 아웃으로 판정합니다. **주자는 자동 추가진루하지 않습니다.** 현재 단순화 규칙에서 희생플라이·태그업은 지원하지 않습니다.
5. 포구하지 못한 공은 첫 바운드와 작은 두 번째 튐/구름을 화면에 표시합니다. 땅에 떨어졌다는 이유만으로 곧바로 안타로 판정하지 않고, 수비수가 공을 확보한 뒤 기존 자동 주루·송구 모델이 아웃 여부를 판정합니다.
6. 우측 상단 격자는 아이가 공을 맞힌 지점과 품질을 보여주는 정보 화면일 뿐이며 새로운 조작이 아닙니다. 투수 모드 CPU 타격에도 같은 타구 생성 코드를 사용하지만 격자 UI는 아이의 타격 모드에만 표시합니다.

## 변경 파일
- `game-v2.js`: 접촉 품질, 다종 타구 생성, 노바운드 포구, 바운드 궤적, 자동 송구와의 시간 연동, 테스트.
- `index.html`: 우측 상단 타격 위치 미니 격자 추가.
- `style.css`: 모바일·데스크톱 대응 투명 카드.

## 검증
- JS 문법 및 모의 DOM/Canvas 초기화, 타격·투수 모드 시작.
- 고정 난수 3,000개 타구: 잘못된 노바운드 포구/바운드 상태 0건, 5가지 형태 확인.
- 각 6개 레벨에서 타구 90개 생성 + 5회 타격·렌더·종료 진행: 총 540개 생성 / 30회 연속 플레이.
- 자기점검 assertion 67개 통과.
- **실제 브라우저 시각 검수는 아직 하지 않았습니다.** GIF/영상/스크린샷 확인 후 최종 병합하십시오.

## 수동 검수 권장
- 땅볼의 첫 바운드가 보이고 플라이 아웃으로 판정되지 않는지.
- 높은 뜬공이 노바운드로 잡힐 때는 플라이 아웃이고 주자가 뛰지 않는지.
- 뜬공이 먼저 바운드하면 수비수가 확보한 후 1·2루 송구 판단을 하는지.
- 1루주자가 2루로 향할 때 1루로 뒤늦게 던지지 않는지, 2루 포구 뒤 1루 병살의 판정이 유지되는지.
- 우측 상단 격자가 중앙 타격 버튼과 겹치지 않는지(특히 320px 모바일 폭).
- 투수 모드 3×3 조준, 포수, 상대 타자 애니메이션이 기존대로 작동하는지.

## 병합 관계
`feature/batted-ball-bounces-and-contact-map`는 `feature/pitching-aim-and-batter`를 부모로 사용합니다. **PR #1의 투수·자동 주루 개선이 먼저 main에 반영되어야** 새 기능을 정상적으로 병합할 수 있습니다.
